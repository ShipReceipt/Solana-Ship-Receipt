{
  "name": "solana-ship-receipt",
  "version": "0.1.0",
  "description": "A signed, reviewer-readable provenance receipt for Solana projects.",
  "type": "module",
  "keywords": [
    "solana",
    "provenance",
    "verification",
    "supply-chain",
    "ed25519"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ShipReceipt/Solana-Ship-Receipt.git"
  },
  "homepage": "https://github.com/ShipReceipt/Solana-Ship-Receipt#readme",
  "bugs": {
    "url": "https://github.com/ShipReceipt/Solana-Ship-Receipt/issues"
  },
  "files": [
    "src/",
    "schema/",
    "docs/",
    "fixtures/",
    "skills/",
    "README.md",
    "LICENSE"
  ],
  "bin": {
    "ship-receipt": "src/cli.mjs"
  },
  "scripts": {
    "check": "npm run check:syntax && npm test && npm pack --dry-run --json",
    "check:syntax": "node --check src/base58.mjs && node --check src/bundle.mjs && node --check src/cli.mjs && node --check src/receipt.mjs && node --check src/viewer.mjs",
    "test": "node --test",
    "test:coverage": "node --experimental-test-coverage --test",
    "sample": "node src/cli.mjs sample",
    "serve:local": "node src/cli.mjs serve sample.receipt.json --port 8787",
    "serve:public": "node src/cli.mjs serve sample.receipt.json --host 0.0.0.0 --port 8787 --network --public",
    "deploy:public": "node src/cli.mjs sample --out sample.receipt.json && node src/cli.mjs serve sample.receipt.json --host 0.0.0.0 --port 8787 --network --public"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
