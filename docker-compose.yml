version: "3.9"

services:
  verifier:
    build:
      context: .
      dockerfile: Dockerfile
    command: >
      sh -c "node src/cli.mjs sample --out sample.receipt.json &&
      node src/cli.mjs serve sample.receipt.json --host 0.0.0.0 --port 8787 --network --public"
    environment:
      NODE_ENV: production
    expose:
      - "8787"
    restart: unless-stopped
    networks:
      - verifier-net

  nginx:
    image: nginx:1.27-alpine
    depends_on:
      - verifier
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./deploy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./deploy/certs:/etc/nginx/certs:ro
    restart: unless-stopped
    networks:
      - verifier-net

networks:
  verifier-net:
    driver: bridge
